{
  "name": "apphub",
  "version": "1.0.0",
  "private": true,
  "description": "AppHub static site + Python proxy server",
  "scripts": {
    "lint": "eslint assets/js/*.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
